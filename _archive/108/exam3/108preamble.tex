%!TEX TS-program = lualatex
%!TEX encoding = UTF-8 Unicode

\documentclass[11pt, addpoints]{exam}

%% Packages
\usepackage[left=0.75in, right=0.5in]{geometry}

\usepackage{fontspec}
\def\mainfont{Linux Libertine O}
\setmainfont[Ligatures=TeX, Contextuals={NoAlternate}, BoldFont={* Bold}, ItalicFont={* Italic}, Numbers={Proportional}]{\mainfont}
\setmonofont[Scale=MatchLowercase]{Inconsolatazi4} 
\setsansfont[Scale=MatchLowercase]{Linux Biolinum O} 
\usepackage{microtype}

\usepackage{graphicx}
	\graphicspath{%
	{/Users/goby/Pictures/teach/108/exams/}}%


\usepackage[parfill]{parskip}
\usepackage{multicol}
\usepackage{amsmath}
%\usepackage{ifthen}
\usepackage{booktabs}
\usepackage[version=3]{mhchem}

\extrafootheight{-1in}
\footer{}{}{\makebox[1in]{\hrulefill}}

\unframedsolutions
\SolutionEmphasis{\bfseries}

% change indentation for multiple choice
\renewcommand{\choiceshook}{%
	\setlength{\leftmargin}{0.25in}%
	\setlength{\parsep}{3pt}%
}

% Used to get the semestesr and last two digits of the year for the header below.
\def\short#1{\csname @gobbletwo\expandafter\endcsname\number#1}
\def\Semester{\ifthenelse{\number\month>8}
	{F}%
	{%
		\ifthenelse{\number\month<4}
		{S}%
		{N}%
	}%
}

\pagestyle{headandfoot}
\firstpageheader{BS 108, Exam 3\ExamCopy{}, \Semester{}\short{\year}}{}{%
	\ifprintanswers\textbf{KEY}\else Name: \enspace \makebox[2.5in]{\hrulefill}\fi}
\runningheader{}{}{\small{pg. \thepage}}

%% Exam class specific items.
%\pagestyle{headandfoot}
%\firstpageheader{\bfseries{BS 108, Exam 3\ExamCopy{}}}{}{%
%	\ifprintanswers\textbf{KEY}\else\bfseries{Name: \enspace \makebox[2.5in]{\hrulefill}}\fi}
%\runningheader{}{}{\small{pg. \thepage}}

\begin{document}

\begin{questions}

\fullwidth{Take a deep breath. Relax. Choose the \emph{best} answer.  Clearly \emph{circle} the letter for your answer.  Vague or unclear marks will be counted as incorrect. Each question is worth 2 points.}
